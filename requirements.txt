# Core dependencies
PySimpleGUI>=4.60.0
cryptography>=41.0.0
bcrypt>=4.0.0
argon2-cffi>=23.0.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
mypy>=1.5.0

# GPU detection (optional - enhances GPX feature)
# Install these for better GPU detection:
# pip install gputil pynvml psutil
gputil>=1.4.0; platform_system != "Linux"
pynvml>=11.5.0; platform_system != "Darwin"
psutil>=5.9.0

# Optional GPU support (uncomment if needed)
# pyopencl>=2023.1
